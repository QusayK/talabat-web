export default class Restaurant {
  constructor(public id: number, public name: string, public city: string,
              public lat: string, public lng: string, public phone: string,
              public image: string) {}
}
